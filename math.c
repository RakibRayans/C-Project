#include <stdio.h>
#include <math.h>
int main()
{
    while (1)
    {

    int n;
    printf("Enter any integer number: ");
    scanf("%d", &n);
    printf("abs(%d) %d\n",n,abs(n));
    }
    return 0;
}
