#include<stdio.h>
int main()
{
    while(1)
    {
        int n,i,j;
        printf("Enter any size : ");
        scanf("%d",&n);
        for(i=n;i<=n;i--)

        {
            for(j=1;i<=n;j++)
            {
                printf(" $ ");
            }
            printf("\n");
        }
    }

    return 0;
}
